# Data Directory

Do not commit downloaded dataset files to this repository.

The Colab notebook loads `erwanlc/cocktails_recipe` at runtime. Each source row becomes one complete RAG document with its title, ingredients, recipe, glass, and garnish.

The project keeps ingredient names exactly as written in the source. It does not classify brands or infer spirit subtypes.
